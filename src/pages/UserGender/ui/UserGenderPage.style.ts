import styled from '@emotion/styled';

const UserGenderPageWrap = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 100dvh;
  background-color: ${({ theme }) => theme.colors.blueGrey[100]};
`;

export const S = {
  UserGenderPageWrap,
};
