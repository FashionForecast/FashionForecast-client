import styled from '@emotion/styled';

import { Button } from '@/shared/ui';

const ActionButton = styled(Button)`
  flex-shrink: 0;
  min-width: 0;
  font-size: 15px;
`;

export const C = {
  ActionButton,
};
