import styled from '@emotion/styled';

const MatchedRegionList = styled.ol`
  margin: 0 16px;
  overflow: hidden;
  border-radius: ${({ theme }) => theme.borderRadius[2]};
`;

export const S = { MatchedRegionList };
